<Window x:Class="MdExplorer.App.Views.SplashWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MdExplorer.App.ViewModels"
        Title="MdExplorer"
        Width="420"
        Height="280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        d:DataContext="{d:DesignInstance Type=vm:SplashViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Border CornerRadius="12"
            Background="#FF1E1E2E"
            BorderBrush="#FF89B4FA"
            BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Image Grid.Row="0"
                   Source="pack://application:,,,/MdExplorer;component/Assets/Logo.png"
                   Width="120"
                   Height="120"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />

            <TextBlock Grid.Row="1"
                       Text="MdExplorer"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="#FFCDD6F4"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0" />

            <TextBlock Grid.Row="2"
                       Text="{Binding VersionText}"
                       FontSize="11"
                       Foreground="#FF9399B2"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0" />

            <TextBlock Grid.Row="3"
                       Text="{Binding StatusText}"
                       FontSize="12"
                       Foreground="#FFA6ADC8"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0" />
        </Grid>
    </Border>
</Window>

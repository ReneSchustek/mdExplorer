<UserControl x:Class="MdExplorer.App.Views.Panels.AllFilesPanel"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MdExplorer.App.ViewModels"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             d:DataContext="{d:DesignInstance Type=vm:AllFilesViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top" Margin="8,8,8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchTextBox"
                     Grid.Column="0"
                     Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Padding="4"
                     ToolTip="Filtert Titel, Pfad und Tags (case-insensitive Teilstring)." />
            <ComboBox Grid.Column="1"
                      Margin="6,0,0,0"
                      Padding="4"
                      SelectedValue="{Binding SortMode, Mode=TwoWay}"
                      SelectedValuePath="Tag"
                      Width="140">
                <ComboBoxItem Content="Zuletzt geändert" Tag="{x:Static vm:AllFilesSortMode.LastModified}" />
                <ComboBoxItem Content="Titel A→Z" Tag="{x:Static vm:AllFilesSortMode.Title}" />
                <ComboBoxItem Content="Pfad A→Z" Tag="{x:Static vm:AllFilesSortMode.RelativePath}" />
            </ComboBox>
        </Grid>
        <Border DockPanel.Dock="Top" Padding="8,0,8,4">
            <TextBlock Foreground="#FF6C7086" FontSize="11">
                <Run Text="Eintraege: " />
                <Run Text="{Binding Items.Count, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>
        </Border>
        <ListBox ItemsSource="{Binding Items}"
                 SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                 BorderThickness="0"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4">
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding RelativePath}" Foreground="#FF6C7086" FontSize="11" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding LastWriteTimeUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm} UTC'}"
                                   Foreground="#FF9CA3AF"
                                   FontSize="10" />
                        <ItemsControl ItemsSource="{Binding TagSlugs}" Margin="0,4,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FFEFF6FF"
                                            BorderBrush="#FFBFDBFE"
                                            BorderThickness="1"
                                            CornerRadius="3"
                                            Padding="4,1"
                                            Margin="0,0,4,2">
                                        <TextBlock Text="{Binding}" FontSize="10" Foreground="#FF1D4ED8" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>

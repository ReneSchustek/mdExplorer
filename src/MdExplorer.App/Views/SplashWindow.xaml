<Window x:Class="MdExplorer.App.Views.SplashWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MdExplorer.App.ViewModels"
        Title="MdExplorer"
        Width="420"
        Height="280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        d:DataContext="{d:DesignInstance Type=vm:SplashViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <!-- Weißer Grund mit dunkelblauer Schrift: Das Logo hat selbst einen weißen
         Hintergrund und saß auf der früheren dunklen Fläche als helle Kachel auf.
         Auf Weiß verschwindet der Rahmen, und es wirkt als eigenes Zeichen. -->
    <Border CornerRadius="12"
            Background="{StaticResource BrandSurfaceBrush}"
            BorderBrush="{StaticResource BrandPrimaryBrush}"
            BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Image Grid.Row="0"
                   Source="pack://application:,,,/MdExplorer;component/Assets/Logo.png"
                   Width="120"
                   Height="120"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />

            <TextBlock Grid.Row="1"
                       Text="MdExplorer"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource BrandPrimaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0" />

            <TextBlock Grid.Row="2"
                       Text="{Binding VersionText}"
                       FontSize="11"
                       Foreground="{StaticResource BrandMutedBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0" />

            <TextBlock Grid.Row="3"
                       Text="{Binding StatusText}"
                       FontSize="12"
                       Foreground="{StaticResource BrandMutedBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0" />
        </Grid>
    </Border>
</Window>
